import { HideFormDirective } from './hide-form.directive';

describe('HideFormDirective', () => {
  it('should create an instance', () => {
    const directive = new HideFormDirective();
    expect(directive).toBeTruthy();
  });
});
